#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: A c program that print z to a
 *
 * Return: Always 0 (success)
*/

int main(void)
{
	char x = 'z';

	while (x => 'a')
	{
		putchar(x);
		x--;
	}
	putchar('\n'');

	return (0);
}
