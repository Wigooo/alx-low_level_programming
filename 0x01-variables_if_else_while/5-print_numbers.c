#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: A c program that types numbers from 0 to 9
 *
 * Return: Always 0 (success)
*/

int main(void)
{
	for (int i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");

	return (0);
}
